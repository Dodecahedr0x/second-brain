# memory/

This directory is intentionally empty.

Operational state (vault index, concept gaps, operation log, user profile) is stored as agent-managed notes directly in the vault. See `context/agent-notes.md` for the convention.
